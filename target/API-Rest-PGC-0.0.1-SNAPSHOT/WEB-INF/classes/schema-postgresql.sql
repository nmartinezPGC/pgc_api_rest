-- Creacion de las Tablas de Parametrizacion de la PGC
-- DROP TABLE IF EXISTS TBL_PAIS;

-- CREATE TABLE TBL_PAIS (
   --ID_PAIS  SERIAL PRIMARY KEY,
   -- COD_PAIS CHARACTER VARYING (5) NOT NULL,
   -- DESC_PAIS CHARACTER VARYING (150) NOT NULL,
   -- INICIALES_PAIS CHARACTER VARYING (5) NOT NULL,
   -- CODIGO_POSTAL CHARACTER VARYING (5),
   -- COD_CONTINENTE CHARACTER VARYING (5),
   -- LATITUD_PAIS CHARACTER VARYING (150),
   -- LONGITUD_PAIS CHARACTER VARYING (150),
   -- CONSTRAINT tbl_pais_pkey PRIMARY KEY (ID_PAIS),
   -- CONSTRAINT uniq_cod_pais UNIQUE (COD_PAIS)
-- );